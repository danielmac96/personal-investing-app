# All data lives in a local SQLite file. Default: <repo>/data/investing.db
# (created automatically on first run). Override only if you keep the DB
# somewhere else.
#INVESTING_DB_PATH=/absolute/path/to/investing.db

# Base URL used for links in the morning email.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
